# Copy to .env

WGER_CODEPATH=/path/to/wger/server
# Host port used to access wger; change it if port 8000 is already in use
WGER_HOST_PORT=8000
# Host port used to access PostgreSQL
POSTGRES_HOST_PORT=5432
# Host port used to access PowerSync
POWERSYNC_HOST_PORT=8080
